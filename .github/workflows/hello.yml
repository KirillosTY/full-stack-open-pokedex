name: Hello World!

on:
  push:
    branches:
      - main

jobs:
  hello_world_job:
    runs-on: ubuntu-24.04
    steps:
      - name: Say hello
        run: |
          echo "Hello World!"
      
      - name: Log Date
        run: | 
          date
      
      - name: Log folder content
        run: | 
        ls
